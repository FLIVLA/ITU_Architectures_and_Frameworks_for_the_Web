/**
 * Verifies user
 * @param {*} firstname 
 * @param {*} lastname 
 * @param {*} password 
 */
function login(firstname, lastname, password){
    if (username === "frederik" && lastname === "lind" && password === "dude")
        return true;
    else return false;
}